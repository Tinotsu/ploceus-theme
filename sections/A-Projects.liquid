<div class="A-Projects">
  <div class="a-inline" style="margin-bottom:5vw;">
    <h2 class="a-h2">Nos meilleurs projets</h2>
    <h4 class="a-h3" style="width: 40%; font-family: var(--type-body-font);">
      Voyez par vous-même ce dont nous sommes capables et imaginez vous à leur place
    </h4>
  </div>
  <div class="a-inline">
    <div class="a-incolumn" style="margin-right: 5vw;">
      <h3 class="a-h4">Tutoriels vidéos personnalisés</h3>
      <h3 class="a-h4">Thème qui leur ressemble</h3>
    </div>
    <div class="a-incolumn">
      <h3 class="a-h4">Outils intégrés et paramétrés</h3>
      <h3 class="a-h4">Satisfactions GARANTIE</h3>
    </div>
  </div>
  <div class="a-carousel-container">
    <div class="a-carousel">
      {% for block in section.blocks %}
        <div
          class="slide"
          onclick="expand(this)"
          style="background-image: url({{ block.settings.slide_image | img_url: '2048x2048' }}); background-size: cover;"
        >
          {{ block.settings.slide_text }}
        </div>
      {% endfor %}
    </div>
  </div>
</div>
<script>
  document.addEventListener('DOMContentLoaded', () => {
    const slides = document.querySelectorAll('.slide');

    const expand = (slide, i) => {
      // Réduire toutes les autres slides
      slides.forEach((it, ind) => {
        if (i !== ind) {
          gsap.to(it, {
            width: '8vw',
            duration: 1,
          });
        }
      });

      gsap.to(slide, {
        width: '42vw',
        duration: 1,
      });
    };

    slides.forEach((slide, i) => {
      slide.addEventListener('click', () => expand(slide, i));
    });
  });
</script>

{% schema %}
{
  "name": "A-Projects",
  "settings": [],
  "blocks": [
    {
      "type": "slide",
      "name": "Slide",
      "settings": [
        {
          "type": "text",
          "id": "slide_text",
          "label": "Slide Text",
          "default": "slide 1"
        },
        {
          "type": "image_picker",
          "id": "slide_image",
          "label": "Slide Image"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "A-Projects"
    }
  ]
}
{% endschema %}
